{
  "name": "@speakwrite/ios",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @speakwrite/web build && npx cap sync ios",
    "open": "npx cap open ios",
    "sync": "npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/cli": "^7.0.0"
  }
}
